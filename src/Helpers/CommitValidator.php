<?php

namespace Sammyjo20\Lasso\Helpers;

class CommitValidator
{
    public static function execute()
    {
        // Todo: git show -s --format=%s
        // Todo: Use the above command to get the current git commit, and if it includes a specific word
        // Todo: We can ignore it.
    }
}
